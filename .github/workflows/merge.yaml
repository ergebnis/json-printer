# https://docs.github.com/en/actions

name: "Merge"

on: # yamllint disable-line rule:truthy
  workflow_run:
    types:
      - "completed"
    workflows:
      - "Integrate"

jobs:
  merge:
    name: "Merge"

    runs-on: "ubuntu-latest"

    timeout-minutes: 5

    if: >
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success' &&
      github.actor == 'dependabot[bot]' && (
        startsWith(github.event.workflow_run.head_commit.message, 'composer(deps-dev)') ||
        startsWith(github.event.workflow_run.head_commit.message, 'github-actions(deps)')
      )

    steps:
      - name: "Request review from @ergebnis-bot"
        uses: "ergebnis/.github/actions/github/pull-request/request-review@4103ff7c010d2c18dc84f72d6704227868412482" # 1.10.0
        with:
          github-token: "${{ secrets.ERGEBNIS_BOT_TOKEN }}"
          reviewer: "ergebnis-bot"

      - name: "Assign @ergebnis-bot"
        uses: "ergebnis/.github/actions/github/pull-request/add-assignee@4103ff7c010d2c18dc84f72d6704227868412482" # 1.10.0
        with:
          assignee: "ergebnis-bot"
          github-token: "${{ secrets.ERGEBNIS_BOT_TOKEN }}"

      - name: "Approve pull request"
        uses: "ergebnis/.github/actions/github/pull-request/approve@4103ff7c010d2c18dc84f72d6704227868412482" # 1.10.0
        with:
          github-token: "${{ secrets.ERGEBNIS_BOT_TOKEN }}"

      - name: "Merge pull request"
        uses: "ergebnis/.github/actions/github/pull-request/merge@4103ff7c010d2c18dc84f72d6704227868412482" # 1.10.0
        with:
          github-token: "${{ secrets.ERGEBNIS_BOT_TOKEN }}"
